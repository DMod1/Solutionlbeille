@model IEnumerable<IbeilleMVC.Models.Enchere>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Montant)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateEnchere)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Article.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Encherisseur.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Montant)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateEnchere)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Article.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Encherisseur.Nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idEnchere }) |
            @Html.ActionLink("Details", "Details", new { id=item.idEnchere }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idEnchere })
        </td>
    </tr>
}

</table>
